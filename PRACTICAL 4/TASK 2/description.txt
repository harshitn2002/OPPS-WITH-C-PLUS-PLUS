Task-2 and Task-4 are combined altogether in this "Task-2"

However, if we use both float values then the compiler will give an error output as both float values cannot be taken,
it converts one float value and one int value or both int values and converts float values to int and operates.
